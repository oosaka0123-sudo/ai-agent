# public/

アプリケーションが生成する公開用アセットを置くディレクトリです。

- `assets/ai/` — `scripts/generate_media.py`（Vertex AIメディア生成基盤）が保存する
  画像・動画の生成物。ファイル自体は `.gitignore` で除外されており、Gitにはコミットされない
  （ディレクトリの存在だけ `.gitkeep` で維持している）。将来、Webサイトへの反映を実装する際は
  ここから必要なファイルを選んで `web/` 側へコピーする想定。
