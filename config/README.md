# config/

アプリケーションの設定ファイル（機密情報を含まないもの）を配置するディレクトリです。

- APIキーやパスワードなどの秘密情報は絶対にここに直接書かないでください。
- 秘密情報は `.env` で管理し、環境変数として読み込んでください（`.env.example` 参照）。
- 環境ごとに設定を切り替える場合は `config.development.yaml` のように命名し、
  必要に応じて `.gitignore` に追記してください。
